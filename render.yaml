services:
  - type: web
    name: floatchat
    runtime: python
    branch: real-world-ocean
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.14